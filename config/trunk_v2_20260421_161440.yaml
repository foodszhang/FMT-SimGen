# U7 full regen: uniform trunk v2, timestamped
_base_: default.yaml

tumor:
  source_type: "uniform"
  radius_range: [2.0, 3.5]
  num_foci_distribution:
    1: 1.0
    2: 0.0
    3: 0.0
  organ_constraint_disabled: false  # voxel-based now (requires mcx_volume_trunk.bin)

dataset:
  num_samples: 1000
  experiment_name: "uniform_trunk_v2_20260421_161440"

quality_filter:
  enabled: true
  min_b_max: 0.001
  min_gt_nonzero_count: 20
  max_retries: 10
